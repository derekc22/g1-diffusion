# Stage 1 Flow Matching Sampling Configuration

root_dir: "/media/learning/DATA/export_smplx_retargeted_sub1_clothesstand"

sample:
  ckpt_path: "/logs/stage1_fm_e500_b128_lr0.0001_w120_s10_transformer_2026Mar29_20-36-16/checkpoints/stage1_fm_epoch_000499.pt"  # Path to Stage 1 FM checkpoint (e.g. logs/stage1_fm_.../checkpoints/stage1_fm_epoch_004999.pt)
  num_samples: 5
  device: "cuda"
  seed: 42
  
  # Flow matching inference settings
  num_inference_steps: 50  # ODE integration steps
  ode_solver: "euler"  # "euler", "midpoint", "rk4"
  
  # Contact constraints
  apply_constraints: true
  contact_threshold: 0.03
  
  # Partial motion generation
  partial_motion_length: 120

dataset:
  window_size: 120
  stride: 10
  min_seq_len: 32
  train: false
  train_split: 0.9
  preload: false
